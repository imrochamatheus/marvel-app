import { MediaType } from "@/types/previewCard.types";
import DefaultContent from "../_content";

export default function CharactersPage() {
  return <DefaultContent mediaType={MediaType.CHARACTER} />;
}
